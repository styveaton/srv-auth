spring:
  application:
    name: srv-auth
  profiles:
    active: dev
  config:
    import: optional:configserver:http://config-server.wa-llet.com
#    import: optional:configserver:http://localhost:8888